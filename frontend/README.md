# Bluewater

This is the frontend for the Bluewater project. An API + site for rowing clubs. This is the frontend project.
The site can either be browsed as a guest (non authenticated) or as logged in user (a member of the accociation).

Currently it's heavily focused on replacing the current https://leden.gyas.nl but the focus is to make it club agnostic.